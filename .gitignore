node_modules/
dist/
build/
coverage/
# Qualquer .env fica de fora; só o .env.example é versionado.
.env
.env.*
!.env.example
*.log
.DS_Store
.vite/
.vercel/

# Lista real de usuários do seeder — contém nomes e, às vezes, senhas.
# O modelo versionado é backend/usuarios.example.json.
usuarios.json

# Planilhas reais do setor: trazem a carteira de clientes, contatos e — no caso
# da Visão Geral — as SENHAS dos portais em texto puro. Nunca versionar; são
# lidas pontualmente pelo import:clientes / atualiza:clientes.
FRPes-*.xlsx
*Visão Geral (Setor Pessoal)*.xlsx
Inclusão de Dados*.xlsx

# Backups do atualiza:clientes — contêm dados de clientes.
backend/backups/
